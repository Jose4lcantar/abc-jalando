import 'package:flutter/material.dart';

class HumidityGraphScreen extends StatelessWidget {
  final String humidity;

  const HumidityGraphScreen({Key? key, required this.humidity}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Aquí deberías retornar el contenido de tu pantalla de gráfica de humedad
    return Container(
      // Contenido de la pantalla de gráfica de humedad
    );
  }
}
